packages:
  # Surrogate keys, schema tests, cross-db helpers.
  - package: dbt-labs/dbt_utils
    version: [">=1.1.0", "<2.0.0"]
  # Distribution / value / row-count expectations (the in-stack, free stand-in
  # for Great Expectations — no separate runtime).
  - package: metaplane/dbt_expectations
    version: [">=0.10.0", "<0.11.0"]
